"""
Suggestion service layer — Phase 5.

Currently minimal: creation is handled directly in the view via the serializer.
This module exists as a placeholder for future business logic (e.g. rate limiting,
duplicate detection, notification triggers).
"""
